"""
Интерфейс экспортёра данных (порт Exporter).

Этот модуль определяет протокол `Exporter`, который используется для реализации
экспортёров данных в различные форматы (JSON, XML и другие).

Интерфейс обеспечивает единый контракт для всех экспортёров и позволяет
легко добавлять новые форматы без изменения остального кода.

Методы:
    dump(data: dict, path: str) -> None:
        Сохраняет переданные данные в указанный файл.
        Параметры:
            data — словарь с данными для сохранения;
            path — путь к файлу, куда нужно записать результат.

Реализации:
    - `JsonExporter` — сохраняет данные в формате JSON;
    - `XmlExporter` — сохраняет данные в формате XML.
"""
from typing import Protocol

class Exporter(Protocol):
    def dump(self, data: dict, path: str) -> None: ...